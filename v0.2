import csv
import os

exampleFile = open('CSV')

exampleReader = csv.reader(exampleFile)
hist = list(exampleReader)

def showRecord(curRec):
  print('#',curRec)
  for a in range(len(hist[curRec])):
    print(str(a)+".",hist[curRec][a])
    
def addRecord(curRec):
  if (len(hist[curRec])>11):
      print('category already added')
  print(categories.values())
  cat = str(input('Please choose category'))
  hist[curRec].append(categories["a"][1:2]+ categories["a"][3:])
  os.system('clear')
  showRecord(curRec)

categories ={"a":"(a)llegro", "z":"(z)akupy", "r":"(r)achunki",
             "o":"r(o)zrywka", "m":"mieszkanie"}

curRec =1
curRecMax = len(hist)-1
print(curRecMax)

showRecord(curRec)

while True:
  os.system('clear')
  showRecord(curRec)
  print ('(a)dd category')
  a = input('(n)ext, (p)revious')
  if (a =='n'):
    if (curRec<curRecMax):
      curRec+=1;
      showRecord(curRec)
    else:
      curRec=1
      showRecord(curRec)
  if (a =='p'):
    if (curRec>1):
      curRec-=1
      showRecord(curRec)
    else:
      curRec = curRecMax
      showRecord(curRec)
  if (a =='a'):
    addRecord(curRec)
      
    
  
    
  
  
